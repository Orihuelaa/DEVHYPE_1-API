// import { useNavigate, useParams } from "react-router-dom";
// import { productos } from "../../../../Data";

const Productos = () => {
//   const {id} = useParams();
//   const navigate = useNavigate();

  return (
    <>
{/* //       <h2>Productos</h2>
//       <button onClick={() => navigate(`/admin/${id}/crear-producto`)}>Crear Producto</button>
//       <ul>
//         {productos.map((producto) => (
//           <li key={producto.id}>
//             <span>{producto.denominacion}</span> - ${producto.precioVenta} 
//             <button onClick={() => navigate(`/admin/${id}/ver-producto/${producto.id}`)}>Ver Producto</button>
//             <button onClick={() => navigate(`/admin/${id}/editar-producto/${producto.id}`)}>Editar Producto</button>
//           </li>
//         ))}
//       </ul> */}
    </>
  );
};

export default Productos;

